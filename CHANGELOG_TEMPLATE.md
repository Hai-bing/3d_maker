# 改动日志模板（CHANGELOG 格式规范）

每次工程改动后，在 `CHANGELOG_SESSION.md` 末尾追加一节，格式如下：

```markdown
## N. YYYY-MM-DD：本次改动主题

### N.1 分类标题（功能 / 修复 / 配置 / 文档 …）
- 改动点描述（涉及文件：`path/to/file`）
- ...

### N.2 另一分类
- ...
```

## 填写约定

- **编号 `N`**：接续上一节数字（当前最后是 `19`，下次写 `20`）。
- **日期**：当天真实日期（`YYYY-MM-DD`）。
- **敏感信息不写入**：API Key、密钥、本机用户名路径一律不写；本机路径用 `~/` 通配或相对路径。
- **与 git 配合**：先补日志，再 `git add -A && git commit -m "描述" && git push`。

## 参考示例（2026-08-14 · 第 19 节）

- 接入 GitHub、`.gitignore` 隐私保护
- 开源脱敏（路径可配置化、MIT LICENSE）
- 启动脚本修复（start.bat 目录校验、启动地址）
- 3D 超时修复（ComfyUI `/free` 释放显存）
- 磁盘清理（删除废弃模型缓存）
